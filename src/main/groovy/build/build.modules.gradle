apply from: rootDir.getPath() + "/buildSrc/outputs/build/build.module.gradle"


buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.2.1"
    }
}

